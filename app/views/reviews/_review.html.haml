%table.table
  %thead
    %tr
      %th{:scope => "col"} #
      %th{:scope => "col"} Ratting
      %th{:scope => "col"} Comment
  %tbody
    - @book.reviews.each_with_index do |review, index|
      %tr
        %th{:scope => "row"}= index
        %td= review.rating
        %td= review.comment
